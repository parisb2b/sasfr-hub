# PROMPT 1 (PRIORITAIRE) — RESTAURER L'ACCÈS AUX DONNÉES (règles Firestore)

> À LANCER EN PREMIER. Objectif : que parisb2b@gmail.com (admin) retrouve immédiatement tout son historique sur total.sasfr.com, sans casser listes.sasfr.com ni dm.sasfr.com. Cargo est traité à part (projet Firebase séparé) et n'est PAS concerné par ce prompt.

---

## CONTEXTE / PROBLÈME

Le projet Firebase **`sasfr-chantiers`** est PARTAGÉ par 3 apps : Comptes Client (total.sasfr.com), Listes (listes.sasfr.com), DM (dm.sasfr.com). Cargo (cargo.sasfr.com) est sur un projet SÉPARÉ (`cargo-optimizer-fca82`) — NE PAS y toucher dans ce prompt.

Symptôme : sur total.sasfr.com, connecté en admin parisb2b@gmail.com, rien ne s'affiche. Console :
```
FirebaseError: Missing or insufficient permissions. [code=permission-denied]
Erreur onSnapshot collaborators (index manquant ?)
```

Cause racine : un `firebase deploy --only firestore:rules` pour Listes a écrasé l'INTÉGRALITÉ des règles du projet partagé, supprimant l'accès de Comptes Client à ses collections.

**Les données NE SONT PAS perdues** (vérifié dans la console Firebase : toutes les collections existent). Problème uniquement de RÈGLES d'accès.

## COLLECTIONS RÉELLES (vérifiées console Firebase, projet sasfr-chantiers)
```
admins                    -> DM
chantiers                 -> DM
comptes_client_sessions   -> Comptes Client  ⚠️ DONNÉES FINANCIÈRES
comptes_client_trash      -> Comptes Client
collaborators             -> Comptes Client (onSnapshot qui échoue actuellement)
listes_lists (+ tasks)    -> Listes
membres                   -> DM
users                     -> PARTAGÉE (auth + champ admin des 3 apps)
```
Inspecter la base réelle ET le code des 3 apps pour recenser TOUTES les collections/sous-collections. Ne pas se limiter à cette liste sans vérifier.

---

## ⚠️ RÈGLES ABSOLUES (données de production sensibles)

1. **SAUVEGARDE OBLIGATOIRE des règles déployées AVANT toute modif.** Récupérer les règles en vigueur, les sauvegarder dans `firestore.rules.backup-AAAAMMJJ-HHMM`. Si impossible via CLI, demander à l'utilisateur de copier-coller depuis la console Firebase (onglet Règles) AVANT de continuer. Ne rien modifier sans cette sauvegarde.

2. **JAMAIS de règle ouverte.** Interdiction de `allow read, write: if true;` ou `if request.auth != null;` sans condition de propriété. Données financières (comptes_client_*) : strictement propriétaire + collaborateurs autorisés + admins.

3. **NE PAS casser Listes ni DM** (qui fonctionnent actuellement). Le nouveau fichier doit couvrir les 3 apps. Lire le code de chaque app (requêtes where/onSnapshot) pour comprendre les accès attendus avant d'écrire.

4. **Fichier unifié et EXHAUSTIF.** La cause du bug = fichier partiel ayant écrasé le tout. Le correctif doit couvrir explicitement chaque collection des 3 apps en un seul firestore.rules complet.

5. **Périmètre strict** : uniquement règles Firestore (+ index si une requête composite l'exige réellement). Ne toucher au code d'aucune app, ne pas modifier les données, ne pas toucher au projet Cargo.

---

## ÉTAPES
1. Sauvegarder les règles actuelles (cf règle 1). Confirmer.
2. Lire le code des 3 apps (dossiers sous C:\DATA-MC-2030\ et/ou repos parisb2b/comptes-client, parisb2b/listes-sasfr, projet DM) pour recenser collections + logique d'accès. Comprendre en particulier la collection `collaborators` et le modèle de partage de Comptes Client (ownerId ? collaborators ? assignees ?).
3. Écrire UN firestore.rules unifié, restrictif par propriété/assignation/rôle admin (admin lu dans users/{uid}.admin == true, ou mécanisme réel à confirmer dans le code). Réintégrer explicitement les règles fonctionnelles de Listes (listes_lists + tasks : créateur OU assigné OU admin) et DM (chantiers, membres, admins).
4. Si une requête nécessite un index composite (ex: collaborators), créer l'index. Mais priorité = règles (le symptôme est permission-denied, pas failed-precondition).
5. Déployer : firebase deploy --only firestore:rules --project sasfr-chantiers (+ indexes si besoin).
6. TESTER EN RÉEL les 3 apps :
   - total.sasfr.com / parisb2b@gmail.com -> comptes + historique réapparaissent, zéro erreur console.
   - listes.sasfr.com -> mes listes / partagées / toutes les listes (admin) OK.
   - dm.sasfr.com -> chantiers, membres OK.
   Si l'une casse, corriger avant de conclure. Les 3 doivent marcher simultanément.

## RAPPORT — MAJ-FIRESTORE-RULES.txt
1. Confirmation sauvegarde des règles précédentes (chemin + contenu)
2. Cause racine confirmée
3. Toutes les collections couvertes + logique d'accès de chacune
4. Confirmation : aucune règle ouverte/publique (surtout comptes_client_*)
5. Index créés le cas échéant
6. Résultat tests réels des 3 apps (chacune confirmée OK)
7. Observations hors périmètre non traitées
